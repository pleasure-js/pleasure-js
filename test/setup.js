import test from 'ava'

test.skip(`Creating a project`, t => {
  /**
   * Using the command line, we are gonna create a *pleasure* project:
   */

})

test.only(`Configuration file: pleasure.config.js`, t => {
  /**
   * The file `pleasure.config.js` located at the top level directory of the project, holds most of the configuration
   * needed in order to have a RESTFul API up and running.
   *
   * This section is divided
   */

  cliTest(`pls create`)
})
test.todo(`Configuration file`)
